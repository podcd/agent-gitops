# Reads named variables out of the agent's own environment and its
# envFile (~/.config/podcd/agent.env, 0600, never in Git).
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: local
spec:
  provider:
    env: {}
